const { application } = require("express")

1. What is Angualr?
    1. Angular is a development platform for building a single page applications for mobile and webkitURL.
    2. It will used for building applications using HTML, CSS and a programming language like a JavaScript or TypeScript.
    3. Angular is not a programming language it is a framework.
     ---------------------------------------------------------    
    Framework:
        1. A framework is like platform for developing software applications.
        2. Framework can have a pre-defined classes functions and methods, those are reusable for several functionalities.A
        3. We don't need to write it from scratch by our own.

2. What is Single Page application?
    1. A single page application is a web application, it loads only one html page when application start.
    2. After that navigation happens without reloading the entire page.
    3. Frameworks like Angular use client side routing to display different types of components dynamically.A
    4. For Example if we client Home to Product, Angular doesn't request a new HTML page from the server, instead of it's replace the home component to product component.
    5. Inside the same page we can use the api's will fetch the data.
    6. So this will be the faster and performance is good.
    -------------------------------------------------------------

3. Why should we use angular?
    1. Angular gives clean and loosely coupled structured
4. Different types of angular?
