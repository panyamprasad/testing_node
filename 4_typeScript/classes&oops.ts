// classes & OOPs
    // A class is blueprint of object creation with properties and methods.
    // In ts classes will support oops concepts like polymorphism, Inheritance, encapslation and abstraction.

// A class can inherit from abother class using extends. it's called inheritance.
// Same method but different implementation it's called as the polymorphism.
// access modifiers:   
    // Total 4 modifiers are there
        // public: we can access anywhere
        // private: we can access inside the class only.
        // protected: we can access classes and sub classes.
        // readonly: can't reassigned after initialization.
