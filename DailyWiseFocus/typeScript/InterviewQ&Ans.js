// --- Interview Questions & Answers

// What is typeScript? Why do we use it?
    // TypeScript is super set of javaScript. It will add the types like String, Number or boolean to javaScript.
    // It will handle the errors in compile time instend of running time.
    // It will make code more secure, maintain and structured.

    // Use case:
        // When we working with large applications, typescript avoid the silly bugs and it will find it at compile time.